# This repository has one maintainer and no required code-owner reviews.
# Leave paths unassigned so automated dependency PRs do not request a review
# that cannot add an independent approval. Add a maintainer team here when one
# exists and require code-owner review in the branch rule at the same time.
